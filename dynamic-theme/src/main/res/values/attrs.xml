<?xml version="1.0" encoding="utf-8"?>
<!--
	Copyright 2019-2021 Pranav Pandey

	Licensed under the Apache License, Version 2.0 (the "License");
	you may not use this file except in compliance with the License.
	You may obtain a copy of the License at

	   http://www.apache.org/licenses/LICENSE-2.0

	Unless required by applicable law or agreed to in writing, software
	distributed under the License is distributed on an "AS IS" BASIS,
	WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
	See the License for the specific language governing permissions and
	limitations under the License.
-->

<resources>

    <!-- Default contrast with color from the theme.
         Generally, it should be a background color so that widgets can change their color
         accordingly if they are background aware. -->
    <attr name="adt_contrastWithDefault" format="color" />

    <!-- Default color control normal color to generate the color state list dynamically. -->
    <attr name="adt_colorControlNormal" format="color" />

    <!-- true to enable dynamic RTL support for the widgets.
         When enabled, widgets will adjust gravity or text alignment automatically to support
         the RTL (right-to-left) layouts. -->
    <attr name="adt_rtlSupport" format="boolean" />

    <!-- Font scaling factor (in percent) for the supported widgets. -->
    <attr name="adt_fontScale" format="integer" />

    <!-- Corner radius to be used by the supported widgets like button, card view, etc. -->
    <attr name="adt_cornerRadius" format="dimension" />

    <!-- Background opacity for the supported widgets. -->
    <attr name="adt_opacity" format="integer" />

    <!-- true to apply window insets. -->
    <attr name="adt_windowInsets" format="boolean" />

    <!-- Color attributes used by the theme and widgets across the whole library. -->
    <declare-styleable name="DynamicTheme">
        <!-- Color options to tint the background dynamically. -->
        <attr name="adt_backgroundColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_color attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to tint the widgets dynamically. -->
        <attr name="adt_colorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_color attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Link color options to tint the widgets dynamically. -->
        <attr name="adt_linkColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_color attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Error color options to tint the widgets dynamically. -->
        <attr name="adt_errorColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_color attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to tint the scroll bar of the scrollable widgets dynamically. -->
        <attr name="adt_scrollBarColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_color attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to change the text color of supported widgets. -->
        <attr name="adt_textColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_textColor
                 attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to change the normal state color of supported widgets. -->
        <attr name="adt_stateNormalColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_itemSelectedColor
                 attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to change the selected state color of supported widgets. -->
        <attr name="adt_stateSelectedColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_itemSelectedColor
                 attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Color options to change the contrast with color of supported widgets. -->
        <attr name="adt_contrastWithColorType" format="enum">
            <!-- Constant for the no color. -->
            <enum name="none" value="0" />
            <!-- Constant for the primary color. -->
            <enum name="primary" value="1" />
            <!-- Constant for the dark primary color. -->
            <enum name="primary_dark" value="2" />
            <!-- Constant for the accent color. -->
            <enum name="accent" value="3" />
            <!-- Constant for the dark accent color. -->
            <enum name="accent_dark" value="4" />
            <!-- Constant for the primary tint color. -->
            <enum name="tint_primary" value="5" />
            <!-- Constant for the dark primary tint color. -->
            <enum name="tint_primary_dark" value="6" />
            <!-- Constant for the accent tint color. -->
            <enum name="tint_accent" value="7" />
            <!-- Constant for the dark accent tint color. -->
            <enum name="tint_accent_dark" value="8" />
            <!-- Constant for the custom color. It must be supplied via adt_contrastWithColor
                 attribute. -->
            <enum name="custom" value="9" />
            <!-- Constant for the background color. -->
            <enum name="background" value="10" />
            <!-- Constant for the tint background color. -->
            <enum name="tint_background" value="11" />
            <!-- Constant for the text primary color. -->
            <enum name="text_primary" value="12" />
            <!-- Constant for the text secondary color. -->
            <enum name="text_secondary" value="13" />
            <!-- Constant for the inverse text primary color. -->
            <enum name="text_primary_inverse" value="14" />
            <!-- Constant for the inverse text secondary color. -->
            <enum name="text_secondary_inverse" value="15" />
            <!-- Constant for the surface color. -->
            <enum name="surface" value="16" />
            <!-- Constant for the tint surface color. -->
            <enum name="tint_surface" value="17" />
            <!-- Constant for the error color. -->
            <enum name="error" value="18" />
            <!-- Constant for the tint error color. -->
            <enum name="tint_error" value="19" />
        </attr>
        <!-- Background aware options to adjust colors accordingly. -->
        <attr name="adt_backgroundAware" format="enum">
            <!-- Constant for the automatic background aware. -->
            <enum name="auto" value="-3" />
            <!-- Constant for the unknown background aware. -->
            <enum name="unknown" value="-2" />
            <!-- Constant to disable the background aware. -->
            <enum name="disable" value="0" />
            <!-- Constant to enable the background aware. -->
            <enum name="enable" value="1" />
        </attr>
        <!-- Theme string that will be applied to the supported views. -->
        <attr name="adt_theme" format="string" />
        <!-- Color value if the background color type is set to custom. -->
        <attr name="adt_backgroundColor" format="color" />
        <!-- Color value if the color type is set to custom. -->
        <attr name="adt_color" format="color" />
        <!-- Alternate color that will be applied to the supported views. -->
        <attr name="adt_altColor" format="color" />
        <!-- Link color value if the color type is set to custom. -->
        <attr name="adt_linkColor" format="color" />
        <!-- Error color value if the color type is set to custom. -->
        <attr name="adt_errorColor" format="color" />
        <!-- Scroll bar color value if the scroll bar color type is set to custom. -->
        <attr name="adt_scrollBarColor" format="color" />
        <!-- Color value if the text color type is set to custom. -->
        <attr name="adt_textColor" format="color" />
        <!-- Color value if the selected item color type is set to custom. -->
        <attr name="adt_stateSelectedColor" format="color" />
        <!-- Color value if the normal state color type is set to custom. -->
        <attr name="adt_stateNormalColor" format="color" />
        <!-- Color value if the contrast with color type is set to custom. -->
        <attr name="adt_contrastWithColor" format="color" />
        <!-- Color alpha value for the supported widgets. -->
        <attr name="adt_colorAlpha" format="integer" />
        <!-- true if the button is borderless to tint it accordingly. -->
        <attr name="adt_styleBorderless" format="boolean" />
        <!-- true to tint background according to the widget color. -->
        <attr name="adt_tintBackground" format="boolean" />
        <!-- Stroke width to be used by the supported widgets like card view, etc. -->
        <attr name="adt_strokeWidth" format="dimension" />
        <!-- true to apply corner radius for surface widgets according to the app theme.
             When disabled, widgets corner radius will be applied according to the default
             implementation. -->
        <attr name="adt_dynamicCornerRadius" format="boolean" />
        <!-- true to enable elevation on the same background for surface widgets.
             When disabled, widgets elevation will be disabled (or 0) if the dynamic theme
             surface color is exactly same as background color. -->
        <attr name="adt_elevationOnSameBackground" format="boolean" />
        <!-- true if the view is floating like popup, dialog, etc. -->
        <attr name="adt_floatingView" format="boolean" />
        <!-- Color for the card background. -->
        <attr name="adt_cardBackgroundColor" format="color" />
        <!-- Corner radius for the card view. -->
        <attr name="adt_cardCornerRadius" format="dimension" />
        <!-- Elevation for the card view. -->
        <attr name="adt_cardElevation" format="dimension" />
    </declare-styleable>
	
</resources>
